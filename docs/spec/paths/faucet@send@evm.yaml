post:
  tags:
    - Send Evm Token
  summary: Send tokens to address on evm chain
  operationId: sendEvm
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - data
          properties:
            data:
              $ref: '#/components/schemas/SendEvm'
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '#/components/schemas/Transaction'
    '400':
      description: invalid request
    404:
      description: chain id or token symbol not supported
    '500':
      description: internal error


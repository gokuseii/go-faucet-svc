post:
  tags:
    - Send Near Token
  summary: Send tokens to address on near chain
  operationId: sendNear
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - data
          properties:
            data:
              $ref: '#/components/schemas/SendNear'
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '#/components/schemas/Transaction'
    '400':
      description: invalid request
    404:
      description: chain not found
    '500':
      description: internal error

